# 资源管理器实际右键命令实施计划

1. 增加隔离的 `IExplorerCommand` 标题/图标探测器和隐藏命令行入口。
2. 扩展应用包清单解析，同时覆盖现代与传统右键处理器声明并统一去重。
3. 将探测结果合并进普通右键管理，失败时提供明确的动态命令兜底名。
4. 增加无厂商特判的解析、去重和标题选择回归测试。
5. 构建验证版与正式版，执行只读/低风险冒烟测试并检查 Beyond Compare 5 的实际显示结果。
