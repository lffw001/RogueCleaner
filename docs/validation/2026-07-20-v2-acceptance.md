# v2 发布前验收记录

日期：2026-07-20

## 结论

当前 v2 单文件核心验收已通过。

已验证：

- v2 可以编译成单文件 `流氓软件克星.exe`。
- 独立目录运行 `--scan-smoke` 成功，退出码 `0`。
- 单文件运行时只在 exe 同目录创建 `流氓软件克星数据`。
- `dist\流氓软件克星` 发布目录只包含 exe 和 README。
- `--acceptance-test` 在独立目录通过 10 个模拟工件验收，退出码 `0`。
- 已验证右键菜单、开机启动、浏览器 Native Messaging Host、文件打开方式、计划任务禁用、服务禁用、恢复中心恢复。
- 已验证“不能静默卸载时弹出原厂卸载器”：工具启动卸载命令并记录 `Launched`，不静默点确认。

仍不承诺：

- 不承诺静默卸载 360、WPS、百度网盘、搜狗、迅雷等主程序或全家桶。
- 有普通卸载命令的疑似捆绑/弹窗/守护组件，只弹出原厂卸载器，由用户自己确认。
- 没有卸载命令或只有异常卸载入口的项目，只报告，不自动删除。
- 真实第三方卸载器的界面、参数和卸载成功与否，必须由用户在弹出的卸载窗口确认；工具不会冒充用户点击。

## 自动验收命令

```powershell
.\流氓软件克星.exe --acceptance-test
```

该命令会创建 `CodexRogueCleanerTest` 前缀的模拟工件，调用正式扫描、清理和恢复核心，并写入：

```text
流氓软件克星数据\reports\acceptance-*.json
```

判定标准：

- `Pass`：扫描命中，清理或禁用后的系统状态符合预期，恢复后状态符合预期。
- `Launched`：卸载类条目已启动原厂卸载命令，工具不静默确认卸载。
- `SetupFailed`：当前系统权限或策略导致模拟工件没创建成功，不能算功能通过。
- `Skipped`：例如服务测试在非管理员状态跳过，不能算服务功能通过。

## 本次独立目录结果

独立运行目录：

```text
D:\Codex\Workspace\RogueCleanerAcceptanceLab-20260720-233735
```

验收摘要：

```text
RunnableCases=10, Passed=10, Failed=0, SetupFailed=0, Skipped=0
```

通过用例：

- 普通文件夹空白处右键：使用360测试右键菜单
- 普通文件右键：WPS 图片测试右键菜单
- 磁盘盘符右键：WPS 云盘/磁盘入口测试
- 开机启动：搜狗弹窗测试项
- 开机启动：迅雷自启测试项
- 浏览器插件/外部宿主：百度网盘看图测试项
- 疑似捆绑组件：不能静默时弹出原厂卸载器
- `.png` 打开方式：百度网盘看图测试项
- 计划任务：360 定时拉起测试项
- 后台服务：360 服务测试项

## 本次修复点

- 注册表值备份文件名加入 `ValueName`，避免同一个父键下多个开机启动项备份互相覆盖，恢复中心能逐项恢复。
- 已禁用的服务和计划任务不再作为待处理项显示，避免“已经禁用但复扫还在列表里”造成误判。
- 疑似捆绑/弹窗/守护组件如果存在普通 `UninstallString`，显示为“弹出卸载器，用户自己确认”。
- 弹出卸载器项不参与“勾选可清理”和“只勾低风险”的批量选择，必须用户手动勾选。
